Place `freshness_model.tflite` here after running `notebooks/03_export_tflite.ipynb`.

The file is ignored by git (too large). In CI/CD, download it from your model
registry before building the app with EAS.
